#loads before application.properties
#It just needs to know: where is the config server

spring.application.name=student-service
spring.cloud.config.uri=http://localhost:8888
#Crash on startup if config server unreachable
spring.cloud.config.fail-fast=true